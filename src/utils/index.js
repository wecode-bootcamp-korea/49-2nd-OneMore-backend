const { getIsInputEmpty } = require("./getIsInputEmpty");
const { throwError } = require("./throwError");
const { getIsPremiumContent } = require("./getIsPremiumContent");

module.exports = {
  getIsInputEmpty,
  throwError,
  getIsPremiumContent,
};
