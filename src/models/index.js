const userDao = require("./userDao");
const exerciseDao = require("./exerciseDao");
const routineDao = require("./routineDao");
const subscriptionDao = require("./subscriptionDao")

module.exports = {
  userDao,
  exerciseDao,
  routineDao,
  subscriptionDao,
};
